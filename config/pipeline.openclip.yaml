version: 1
engines:
  media_ingestor: mock
  captioner: mock
  ocr: mock
  embedder: openclip_vitb32
  categorizer: openclip_zeroshot
  prioritizer: rules_v1
  verifier: openclip_similarity
storage:
  type: local
  uploads_dir: data/uploads
  artifacts_dir: data/artifacts
api:
  category_top_k: 3

categorization:
  confidence_method: softmax
  softmax_temperature: 0.25
